// Deprecated mobile page variant
// The application uses `src/app/page.tsx` -> `src/app/home.tsx`. This file is
// kept only for archival reasons. It no longer renders a page component.

export default function DeprecatedMobilePage(): null {
  return null;
}